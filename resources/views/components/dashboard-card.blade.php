<div class="small-box {{ $type }} shadow-sm rounded">
    <div class="inner d-flex justify-content-between align-items-center">
        <div>
            <h3 class="mb-1">{{ $value }}</h3>
            <p class="mb-0">{{ $label }}</p>
        </div>
        <div class="icon">
            <i class="{{ $icon }}" style="font-size:48px; opacity:0.4;"></i>
        </div>
    </div>
</div>
